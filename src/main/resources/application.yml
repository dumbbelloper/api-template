server:
  port: 8080
  servlet:
    context-path: /

spring:
  # todo ?? ??? ??? ??
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
      # create ?????? ?? ? ?? ??? ?? ? ??? ?? ?? - ?? ??
      # create-drop ?????? ?? ? ??? ?? ? ?????? ?? ??? ??? drop - ?? ??
      # update ??? ??? ?? - ?? ??
      # validate ???? ??? ?? ?? ?? - dev, prod
      # none ???? ?? - dev, prod
    show-sql: true # console print sql
    properties:
      hibernate:
        format_sql: true # console print sql ?? ??
        default_batch_fetch_size: 500 # OneToMany ??? ?? ? ?? ???. where in (1, 2, 3, ..., 500)
    open-in-view: false # ??? ????? ?? ??? ???? ??? ??(???, ?????)
  servlet:
    multipart:
      max-file-size: 10MB # ?? ??? ?? ? ?? ??
      max-request-size: 100MB # ?? ??? ?? ? ?? ?? ?? ?? 100MB ??
logging:
  level:
    org.hibernate.type: trace # ???? ??? ?? ? ? ?? ?? ??
    com.app: debug
feign:
  client:
    config:
      default:
        connect-timeout: 5000
        read-timeout: 5000

kakao:
  client:
    id: 64b28a61aea45be430a343ed6768e7b5
    secret: H6lOOdKRopk90cLZWsczwhaZCVVqPHpC
    redirect-uri: http://localhost:8080/oauth/kakao/callback

token:
  secret: and0LXRva2VuLXNlY3JldA== # 임의의 문자열
  access-token-expiration-time: 900000 # 15분 1000(ms) x 60(s) x 15(m)
  refresh-token-expiration-time: 1209600000 # 2주 1000(ms) x 60 (s) x 60(m) x 24(h) x 14(d)